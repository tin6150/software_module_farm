echo Will should exec these scripts...
find ./noarch/modules/ -name Install.sh -exec ls   -ld {} \;
echo "Press Enter to continue, ^C to cancel"
read UserInput
echo "running...   untested....  activation tbd..."
#echo find ./noarch/modules/ -name Install.sh -exec bash -c  {} \;

